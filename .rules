# Waydoodle

This is a Wayland application implemented in Rust that allows the user to
annotate on the screen (e.g. for presentations).

## Conventions

- Avoid writing top-level comments separating sections like:

  ```
  // ---------------------------------------------------------------------------
  // Event‑loop thread
  // ---------------------------------------------------------------------------
  ```
